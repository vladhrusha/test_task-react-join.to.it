/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/test_task-react-join.to.it",
  output: "export",
  reactStrictMode: true,
  typescript: {
    ignoreBuildErrors: true,
  },
};

export default nextConfig;
